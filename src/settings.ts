/**
 * This is the name of the platform that users will use to register the plugin in the Homebridge config.json
 */
export const PLATFORM_NAME = 'PlexWebhooksPlatform';

/**
 * This is the name of the plugin as defined the package.json `name` property
 */
export const PLUGIN_NAME = 'homebridge-plex-webhooks-testing';